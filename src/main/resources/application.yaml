# Database connection service
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Vetrina
    username: root
    password:
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
    name: vetrina
# Security Login
